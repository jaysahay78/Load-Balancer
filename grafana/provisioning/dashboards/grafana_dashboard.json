{
  "annotations": { "list": [] },
  "editable": true,
  "graphTooltip": 0,
  "panels": [
    {
      "type": "timeseries",
      "title": "Requests/s",
      "datasource": { "type": "prometheus", "uid": "PROMDS" },
      "targets": [{ "refId": "A", "expr": "sum(rate(lb_requests_total[1m]))", "legendFormat": "RPS" }],
      "fieldConfig": { "defaults": { "unit": "req/s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 0 }
    },
    {
      "type": "timeseries",
      "title": "Upstream latency (p50/p90/p99)",
      "datasource": { "type": "prometheus", "uid": "PROMDS" },
      "targets": [
        { "refId": "P50", "expr": "histogram_quantile(0.50, sum(rate(lb_upstream_latency_bucket[5m])) by (le))", "legendFormat": "p50" },
        { "refId": "P90", "expr": "histogram_quantile(0.90, sum(rate(lb_upstream_latency_bucket[5m])) by (le))", "legendFormat": "p90" },
        { "refId": "P99", "expr": "histogram_quantile(0.99, sum(rate(lb_upstream_latency_bucket[5m])) by (le))", "legendFormat": "p99" }
      ],
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 8 }
    }
  ],
  "schemaVersion": 38,
  "style": "dark",
  "tags": ["java","load-balancer","micrometer"],
  "templating": {
    "list": [{ "type": "datasource", "name": "PROMDS", "label": "Prometheus", "query": "prometheus", "hide": 0 }]
  },
  "time": { "from": "now-6h", "to": "now" },
  "title": "Java LB — Overview",
  "version": 1
}
